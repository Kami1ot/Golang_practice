package main

// HalfSums считает суммы левой и правой половин слайса в двух горутинах.
// Левая — nums[:len/2], правая — nums[len/2:].
func HalfSums(nums []int) (int, int) {
	// TODO: две горутины, каждая пишет в свою переменную; Wait перед return
	return 0, 0
}
