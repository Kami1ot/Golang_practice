package main

// Divide возвращает частное a/b, а при b == 0 — ошибку "деление на ноль".
// Понадобится errors.New — добавьте import "errors".
func Divide(a, b int) (int, error) {
	// TODO: guard-проверка нуля с ранним выходом, потом счастливый путь
	return 0, nil
}
