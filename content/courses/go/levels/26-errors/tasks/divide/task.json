{
  "id": "divide",
  "type": "unittest",
  "title": "Деление с ошибкой",
  "required": true,
  "order": 1,
  "timeoutSec": 15,
  "hints": [
    "Каркас прямо из урока: сначала guard-проверка `if b == 0` с ранним выходом, потом счастливый путь `return a / b, nil`.",
    "Ошибку создайте через `errors.New(\"деление на ноль\")` (нужен `import \"errors\"`). Текст — в точности как в условии: с маленькой буквы, без точки."
  ]
}
