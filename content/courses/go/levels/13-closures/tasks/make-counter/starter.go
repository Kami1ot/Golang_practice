package main

// MakeCounter возвращает функцию-счётчик: первый вызов — 1,
// второй — 2 и так далее. Каждый счётчик считает независимо.
func MakeCounter() func() int {
	// TODO: заведите переменную-счётчик и верните замыкание,
	// которое увеличивает её и возвращает
	return nil
}
