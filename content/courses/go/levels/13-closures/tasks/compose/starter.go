package main

// Compose возвращает функцию h, такую что h(x) == f(g(x)):
// сначала применяется g, потом f — как в математике f∘g.
func Compose(f, g func(int) int) func(int) int {
	// TODO: верните анонимную функцию, которая применяет g, затем f
	return nil
}
