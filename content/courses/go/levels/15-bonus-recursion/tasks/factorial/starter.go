package main

// Factorial возвращает n! — произведение целых чисел от 1 до n.
// По определению Factorial(0) = 1. Считайте, что 0 <= n <= 12.
func Factorial(n int) int {
	// TODO: базовый случай для n == 0, затем рекурсивный шаг
	return 0
}
