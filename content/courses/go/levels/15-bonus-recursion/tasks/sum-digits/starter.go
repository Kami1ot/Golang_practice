package main

// SumDigits возвращает сумму цифр неотрицательного числа n.
// Решение должно быть рекурсивным: базовый случай — n < 10.
func SumDigits(n int) int {
	// TODO: однозначное число вернуть как есть; иначе отделить последнюю цифру
	return 0
}
