package main

// First возвращает первый элемент слайса.
// Пустой слайс — не паника, а ошибка "пустой слайс".
func First(nums []int) (int, error) {
	// TODO: guard-проверка длины перед обращением по индексу
	return 0, nil
}
