{
  "id": "move-point",
  "type": "unittest",
  "title": "Подвинуть точку",
  "required": true,
  "order": 2,
  "timeoutSec": 15,
  "hints": [
    "К полям структуры через указатель обращаются напрямую: `p.X` — Go разыменует сам, `(*p).X` писать не нужно.",
    "Move — это два присваивания с `+=` (к X прибавить dx, к Y прибавить dy); Reset — записать в оба поля ноль. Изменения по указателю видны снаружи автоматически."
  ]
}
