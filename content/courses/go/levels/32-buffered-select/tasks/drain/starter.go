package main

// Drain забирает из канала всё, что доступно прямо сейчас,
// и возвращает слайсом. Пустой канал — мгновенный пустой результат.
// Закрытый канал не должен зацикливать функцию (ok-идиома!).
func Drain(ch <-chan int) []int {
	res := []int{}
	// TODO: цикл вокруг select с default
	return res
}
