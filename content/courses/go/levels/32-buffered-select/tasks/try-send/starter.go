package main

// TrySend пытается отправить v в ch без блокировки:
// удалось — true, канал не готов — false.
func TrySend(ch chan int, v int) bool {
	// TODO: select с веткой-отправкой и default
	return false
}
