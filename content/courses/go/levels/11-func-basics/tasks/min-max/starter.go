package main

// MinMax возвращает минимум и максимум из трёх чисел — именно в этом порядке.
// Не меняйте имя и сигнатуру: тесты ищут MinMax(a, b, c int) (int, int).
func MinMax(a, b, c int) (int, int) {
	// TODO: заведите кандидатов на минимум и максимум,
	// сравните их с остальными числами через if
	return 0, 0
}
