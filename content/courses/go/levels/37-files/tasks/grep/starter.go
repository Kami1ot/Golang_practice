package main

// Grep переносит из файла srcPath в файл dstPath только строки,
// содержащие substr, и возвращает их количество.
// Исходного файла нет — верните 0 и ошибку открытия.
// Понадобятся os, bufio, fmt и strings — добавьте импорты сами.
func Grep(srcPath, dstPath, substr string) (int, error) {
	// TODO: Open + Scanner по источнику, Create + bufio.NewWriter в приёмник,
	// strings.Contains для фильтра и обязательный Flush в конце
	return 0, nil
}
